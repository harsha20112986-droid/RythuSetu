# Tests

Tests will be added alongside each feature module.

Initial target:

- Backend health/API smoke tests
- Scheme eligibility rule tests
- Benefit estimator calculation tests
- Crop-loss validation tests
- Frontend critical-flow tests

Rule calculations should be deterministic and covered with edge cases before AI explanations are added.
